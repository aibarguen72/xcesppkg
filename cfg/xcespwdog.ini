# /var/xcesp/cfg/xcespwdog.ini — xcespwdog production configuration
# xcespwdog supervises xcespserver and xcespproc.
# Run via:  systemctl start xcesp

[WDOG]
# xcespwdog is the central log collector for the whole stack
LOG_CONSOLE_ENABLED     = true
LOG_CONSOLE_LEVEL       = Info
LOG_FILE_ENABLED        = true
LOG_FILE                = /var/xcesp/log/xcesp.log
LOG_FILE_LEVEL          = Info
# LOG_FILE_CLEANSTART   = false  (true = truncate log on each start)

# UDP syslog listener: xcespserver and xcespproc forward their logs here
LOG_LOCAL_PORT          = 1514

# process.1 — xcespserver: no heartbeat timers (server runs until told to stop)
[process.1]
WDOG_LAUNCH             = /usr/bin/xcespserver --config /var/xcesp/cfg/xcespserver.ini
WDOG_START_TIMER        = 0
WDOG_INTERVAL_TIMER     = 0
WDOG_RESPAWN            = true
WDOG_RESPAWN_MAXCOUNT   = 3

# process.2 — xcespproc instance 1 (affinity 1)
# -s 30: send SIGUSR1 heartbeat to xcespwdog every 30 s
# Add -p 1514 to forward xcespproc logs through xcespwdog's syslog listener
[process.2]
WDOG_LAUNCH             = /usr/bin/xcespproc --config /var/xcesp/cfg/xcespproc.ini -i 1 -s 30
WDOG_START_TIMER        = 60000
WDOG_INTERVAL_TIMER     = 60000
WDOG_RESPAWN            = true
WDOG_RESPAWN_MAXCOUNT   = 3

# process.3 — xcespproc instance 2 (affinity 2, optional — remove if not needed)
# [process.3]
# WDOG_LAUNCH             = /usr/bin/xcespproc --config /var/xcesp/cfg/xcespproc.ini -i 2 -s 30
# WDOG_START_TIMER        = 60000
# WDOG_INTERVAL_TIMER     = 60000
# WDOG_RESPAWN            = true
# WDOG_RESPAWN_MAXCOUNT   = 3
