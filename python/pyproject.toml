[build-system]
requires = ["setuptools>=45"]
build-backend = "setuptools.build_meta"

[project]
name = "xcesppy"
version = "0.0.2"
description = "XCESP Python client library"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
include = ["xcesppy*"]
